#include "main.h"

/**
 * add - add two integers and return result
 *
 *@a: first number to do the add
 *@b: second number to do the add
 *
 * Return: the result of a + b
 */

int add(int a, int b)
{
	int result = (a + b);

	return (result);
}
