#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int number = 0;

	/* Display 123456789 and stop before 10 */
	for (number = 0 ; number < 10 ; number++)
	{

		printf("%d", number);

	}
	putchar('\n');
	return (0);
}
